//Tipos de dados primitivos: string, number, undefined, null, boolean

const nome = 'Vitória'; //string
const nome1 = "Vitória"; //string
const nome2 = `Vitória`; //string

const num1 = 10 //number
const num2 = 10.52 //number

let nomeAluno; // undefined pois não foi inicializado, ou seja, não aponta lugar nenhum na memória
let sobrenomeAluno = null; // nulo = não aponta lugar nenhum na memória

const aprovado = true; //boolean = true or false (lógico)

console.log(typeof aprovado)